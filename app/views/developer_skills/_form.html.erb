<%= form_for(@developer_skill) do |f| %>
  <% if @developer_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@developer_skill.errors.count, "error") %> prohibited this developer_skill from being saved:</h2>

      <ul>
      <% @developer_skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :language %><br />
    <%= f.text_field :language %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
